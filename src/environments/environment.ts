export const environment = {
  // Relative path: in development, `ng serve` proxies /api/* to FastAPI on
  // http://127.0.0.1:8000 (see proxy.conf.json), so no CORS setup is needed.
  apiBaseUrl: '/api',
};
